import styles from '@/app/dashboard/inventory/styles.module.scss';

const InventoryPage = () => {
  return (
    <div className={styles.mainContainer}>
      <h6>Message page</h6>
    </div>
  );
};

export default InventoryPage;
